# DevOop_Project Git Workflow

Step 1: Initialize Git Repository
mkdir DevOop_Project
cd DevOop_Project
git init

Step 2: Link Local Repository to GitHub
git remote add origin <your-repo-url>

Step 3: Set Up Branches
git branch -M main
git checkout -b develop
git checkout -b feature/database-servicee
git checkout -b feature/cache-service

Step 4: create compose.yml file 
 write database and cache command insite it

Step 5: merge into develop branch
   go to develop branch
   git checkout develop
   git merge feature\cache-service

   Step 6: push develop and main branch

   Step 7:  git setup file update 

   step 8:


